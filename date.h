#pragma once
#include <string>
using namespace std;
//date.h/cpp — эти файлы должны содержать объявления и определения класса Date,
//функции ParseDate и оператора вывода в поток для класса Date;
class Date {
public:
	
};


string ParseDate(istream& stream);
